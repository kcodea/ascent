# 2026-08-26 — Doc Bot: the standing correctness auditor

Built the on-ramp to the owner's Interaction Verification Lab blueprint (Codex, 2026-06-29): eight registry
tripwires (1–4 from the day's reports; 5–8 mined from ~480 historical fix commits — reference integrity #719,
turn-scoped resets #517/#670, the rune-reward differential #900, spell-power folding #817/#731) + `npm run docbot`, named **Doc Bot**. Doctrine in [docs/docbot.md](../docbot.md).

Motivation: the day's nine owner-reported bugs fell into five structural classes, each predictable without
gameplay judgment — silent factory-map dispatch (`MAP[do]?.()`), half-blind live text, raw tribe compares,
diverging "mirrors X" code paths, and gated combat casts that fizzle. Doc Bot re-derives each question from
live content/source every CI run, with excuse registries (reasons required) instead of loosened assertions.

Landing it immediately produced:
- **16 needs-triage phase gaps** (ratcheted; the report is the owner's ruling queue) — several look like real
  bugs of the Funeral-on-Loan class (`deathrattleBuffShopPermanent`, `deathrattleTriggerAdjacentRally`).
- **One live divergence fixed in the PR**: `snapshotBoard.beastsPlayed` still used a raw tribe compare after
  #1216 fixed the reducer — a served board's Pack Leader fought weaker than its owner's (patch-noted).
- **A burn-down target**: `arena.ts` has 13 raw tribe sites, zero all-types guards, and serves both phases.
- **A recorded false positive**: inlined `castRepeat` casts (Watcher/Lantern of Souls) bypass the
  `combatCastable` gate by design — only `castNamedSpell` routes are gated.

All four tripwires sabotage-checked (reintroduced Kringle's exact bug, deleted an excuse, added a raw
compare, reverted the snapshot fix — each caught with a named diagnostic).

The history mining also produced two new owner queues: **80 runes whose second copy silently does nothing**
(reachable — the forge never excludes owned runes and Duplication doubles any Epic; ratcheted pending
per-rune duplicate-policy rulings) and two stat-spell factories with an unruled spell-power question.
Instrument honesty: tripwire 7's own sabotage check caught two blind spots in itself before they could lie
(JSON key-order noise; `flagCopies` masking) — both recorded in the test header.

**Behavioural layer (same day, owner ask "do the entire improvement scope"):** tripwires 9–12 — play/cast/
watcher differentials through the real reducer, combat presence differentials through the real simulate()
(the generic Conductor catcher), printed-number agreement (292 params, 0 misses), and the reducer invariant
fuzz (determinism + modal-deadlock + identity-independence). Three instrument sagas recorded in the code:
the control-body hunt (Drakko → Sylus → zero clean non-token minions exist — `omen` the token, validated
per-run), event-bookkeeping masking, and the sanctioned shallow-clone purity ruling. New owner queues: 54
scenario-conditional combat effects, 27 golden-flat combat cards.

**Limitation build-out (same day, owner ask "fix these limitations"):** tripwires 13–15 + lane upgrades —
combat variant matrix (inert queue 54 → 7, golden-flat honest at 61), magnitude oracles (grants EQUAL
params for three families incl. `fixed`/`goldenTokens` semantics), interaction matrix (multiplier × family
exact doubling, additivity metamorphic, 10-seed eligibility sweeps), hero-power lane (34 active, 25 pinned by
kind), serialize-resume equivalence and presentation parity in the fuzz. Roadmap status table updated in
docs/docbot-roadmap.md; seven more instrument catches recorded in code.

**Retro-validation (same day, owner challenge "so items 1–4 push us to 85%?"):** built the reinjection
harness (packages/tools/retro/) and MEASURED instead of estimating: 8 out-of-sample historical bugs
reinjected at the source line — **0 of 8 caught** by the estimate-era suite. The misses built tripwires 16
(the 135-key QuestCombatMods lane — an entirely uncovered surface holding 3 of the 8) and 17 (copy
semantics, watcher ordering, chain multipliers, combat-castable registry). Re-measured: **7 of 8 caught**,
with #1176 an honest standing miss. Measured table in docs/docbot-roadmap.md replaces the 65–75% estimate.

**Catalog wave 2 (owner: "continue"):** six more historical reinjections — two coverage confirmations (the
spell-power scan; the refused-spells pin catching through its LOWER bound) and four new misses that built
tripwire 18 (all-types combat auras, the reducer-side targetTribe gate, the maxGoldBonus channel, self-target
identity). Standing: **13 of 14 catalog bugs caught**, #1176 the honest miss.

**Rulebook + triage board (owner chain, after #1218 merged):** `@game/rules` registry — schema, the five
handoff-confirmed owner rulings as `approved`, `rules:seed` generating a 274-item pending backlog from Doc
Bot's queues — plus the clickable board (DEV MENU → Rulebook Triage): Approve/Revise/Reject per rule,
writing `decisions.json` through a dev-server endpoint (`rulebookPlugin`, beatLabPlugin conventions, pure
validation unit-tested + endpoint E2E'd through the vite API). Doc Bot's hero scan extracted to
`heroScan.ts` so the seeder and the lane share one implementation.
