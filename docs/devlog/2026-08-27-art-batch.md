# 2026-08-27 — Art batch: Gifts, heroes, hero powers, runes

Owner ask 2026-08-27: sweep four master folders under `C:\Game Assets\Ascent Art` and wire everything whose
FILENAME name-matches content (standing ruling: never guess from an un-attributed file). Result: **10 files
wired** (7 Gifts + 2 runes + Aevor's replacement portrait); everything else was either already wired from
earlier batches or is reported below as unmatched.

No `art.ts` change was needed anywhere: Gifts are cards, so their art rides the existing `art/spells/`
channel (`artFor` falls through minions → spells); runes/heroes were already globbed.

## Batch 1 — Gifts (`Ascent Art/Gifts`, 14 masters)

Matched on card NAME (normalized) → card id, wired into `packages/ui/src/art/spells/<id>.webp`.

| Master | Card id | Status |
|---|---|---|
| FastTrack.png | gift_fast_track | **wired** |
| FriendsAndFamily.png | gift_friends_family | **wired** |
| GraveInvitation.png | gift_grave_invitation | **wired** |
| PartingGifts.png | gift_parting_gifts | **wired** |
| SecondCalling.png | gift_second_calling | **wired** |
| SpecialDelivery.png | gift_special_delivery | **wired** |
| UnbridledMight.png | gift_unbridled | **wired** (the old `UnbridledWrath.png` near-miss is resolved — this master names the card exactly) |
| ArcaneClearance / ChampionsRegalia / DemandAnEncore / IroncladFavor / PerfectPick / PremiumStock / RoyalAllowance | gift_arcane_clearance / gift_regalia / gift_encore / gift_ironclad / gift_perfect_pick / gift_premium_stock / gift_allowance | already wired (2026-08-26 batch) |

Still artless: **gift_larceny (Grand Larceny)** — no master exists. It is the sole remaining entry in
`ART_PENDING` (`allTypesPill.test.ts`); the other 8 Gift entries were deleted as their art landed.

## Batch 2 — Heroes (`Ascent Art/Heroes`)

Every live hero already had a portrait; the diff (all 59 `HEROES` ids vs `art/heroes/`) found **zero heroes
with a master and no repo art**. The one action: **Aevor's NEW portrait** — deleted the stale
`art/heroes/aevor.webp` and regenerated from `Aevor.png` (the webp-wins trap: a new PNG beside an old webp
silently loses).

Skipped / reported (NOT wired — no name match):

| Master | Why skipped |
|---|---|
| 45896d1b-…, 4b432fcd-…, 5140c942-…, daf4fb8c-….png | UUID-named, un-attributed — owner to identify |
| AasterTheGuide.png | near-miss typo (double-A) for Aster the Guide; `aster.webp` already wired, so nothing missing — but flagging the name |
| Kasken.png | no hero id or display name matches ("keshi"? — not assumed) |
| Orwin.png (+ OrwinHP.png in Hero Powers) | no `orwin` hero exists in `heroes.ts` — presumably an upcoming hero |
| Rayze.png | spelled with a Z; the hero is `rayse` and already has art — near-miss, not re-wired |

(`art/heroes/herald.webp` exists with no matching hero def — pre-existing, untouched.)

## Batch 3 — Hero powers (`Ascent Art/Heroes/Hero Powers`)

Every live hero already has power-button art; every `<Hero>HP.png` master is already wired (earlier batches
came from this same folder). Nothing to wire. Reported:

| Master | Why skipped |
|---|---|
| CassenBridge / CassenCastle / CassenHouse / CassenShed / CassenZeppelin.png | Cassen's commission art is keyed by commission KIND (`cassen-gold/spell/discover/fortress/citadel`, read by `StatusBar.tsx`). These five masters are named by BUILDING, and no building→kind mapping exists anywhere — wiring them would be a guess. Needs the owner to say which building is which commission. |
| CiaAce/Clubs/Diamonds/Hearts/Spades, FlashFirst/Last | already wired (`cia-*`, `flash-*`) |
| CindaraWhelp.png | already wired as the token's MINION art (`art/minions/cindarawhelp.webp`, 2026-08-23) — not a power key |
| Pathfinder.png | no hero, power, or variant key named Pathfinder — un-attributed |
| OrwinHP.png | no `orwin` hero (see Batch 2) |

## Batch 4 — Runes (`Ascent Art/Runes`, 301 top-level masters + `Special Rune Rewards/`)

Matched on rune NAME normalized (strip spaces/punctuation, case-fold; `RuneOThe…` → "Rune o' the …",
`RunrOf…` typo folded). Against 295 rune defs and 291 existing arts:

- **Wired 2**: `RuneOfHappyBirthday.png` → `rune_happy_birthday`, `RuneOfMerryChristmas.png` →
  `rune_merry_christmas` (the two new Gift runes).
- **Already wired: 273** masters matched runes that had art.
- **No master: 2** runes remain artless — `rune_living_magic` (Rune of Living Magic), `rune_wild_memory`
  (Rune of Wild Memory). Note the unmatched `RuneOfWildMagic.png` below sits BETWEEN those two names — a
  blend of both, matching neither exactly. Deliberately not guessed; owner to confirm which (or neither).
- **Unmatched masters: 26** (no rune def bears the name — mostly renamed/archived/upcoming):
  ee0b0864-….png (UUID), RuneOfEchoingCry, RuneOfGilding, RuneOfTheAftermath, RuneOfTheBackbeat (the rune is
  named without "the"? — its art `rune_backbeat.webp` already exists), RuneOfTheBasicDragons (plural;
  `rune_basic_dragon` arted), RuneOfTheBeastialSwarm (`rune_beastial_swarm` arted), RuneOfTheCaravan,
  RuneOfTheCarrionCoin (`rune_carrion_coin` arted), RuneOfTheCounterpoint, RuneOfTheDragonscale,
  RuneOfTheDragonsPantry, RuneOfTheEchoedArrival, RuneOfTheEmberline, RuneOfTheEngravingGems,
  RuneOfTheFinality, RuneOfTheFinalVerse, RuneOfTheGolems, RuneOfTheHoardflame, RuneOfTheMerchantsChorus,
  RuneoftheMotherload (typo twin of the matched Motherlode), RuneOfTheRubyShrapnel, RuneOfTheRunicExchange,
  RuneOfTheSellersMarket, RuneOfWildMagic (see above), SpellOfPillaging (a spell, not a rune).
  Several of these differ from an arted rune only by an added "The"/plural — those runes already have art
  from earlier masters, so nothing is missing; the files are just alternate/legacy names.

## Bookkeeping

- `allTypesPill.test.ts` `ART_PENDING` shrunk to `gift_larceny` only.
- `artNoRedundantMasters.test.ts` file budget raised 1031 → 1040 (net +9 files; Aevor replaced, net 0).
- Patch note prepended (`UI / Info`, 2026-08-27 "Art Batch").
- Reminder: the art globs are eager — a running dev server needs a RESTART (not a reload) to see new files.
