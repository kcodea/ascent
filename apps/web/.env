# Shared Supabase board backend — committed so every dev (and the itch build) gets it with zero setup.
# These are SAFE to commit: the publishable key is public by design (it ships in the client bundle); the
# data is protected by Row Level Security, not by hiding the key. NEVER commit the `secret`/`service_role`
# key. Personal overrides go in `.env.local` (gitignored). See docs/board-backend.md.
VITE_SUPABASE_URL=https://zcwhbejpqcdcfdpfxeza.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_6wDY3YIqV4ss1LBg2c5_Pg_suBrERDK
